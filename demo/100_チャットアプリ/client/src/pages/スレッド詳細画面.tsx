/**
 * スレッド詳細画面。
 *
 * スレッド直下のメッセージ1件をトップとして、
 * それに対する返信の一覧を表示する。
 * 作成時刻が古いものが上、新しいものが下の順番で表示される。
 * 特に件数は絞られず、全部一度に読み込む。
 *
 * 画面下部には新規メッセージ入力フォームがある。
 * ユーザーがメッセージを入力して送信ボタンを押すと、
 * このスレッドのトップのメッセージに対する返信が作成される。
 */
export default function () {
  return (
    <div>
      TODO: スレッド詳細画面
    </div>
  )
}