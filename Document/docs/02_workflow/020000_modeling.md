---
sidebar_position: 2
---

# モデリングの基礎

Nijoにおける開発の核心は「モデリング」にあります。
コードを書き始める前に、アプリケーションが扱うデータ構造と処理を「モデル」として定義し、そこから堅牢な土台を自動生成します。

## 3つの主要モデル

Nijoでは、**DataModel**、**QueryModel**、**CommandModel** という3つの主要なモデルが協調してアプリケーション全体を構成します。

* **[DataModel](./020300_DataModel/index.md)**
  * **永続化されるデータの形** です。
  * RDBMSのテーブル定義や、トランザクションの境界に相当します。

* **[CommandModel](./020500_CommandModel/index.md)**
  * **アプリケーションの利用者や外部システムが要請を行い、結果を返す1サイクル** の定義です。
  * 入力（引数）を受け取り、処理を行い、出力（戻り値）を返します。
  * データの登録・更新だけでなく、「複雑なデータ構造をもつ画面の初期表示処理」や「データ集計Excel出力」といった操作もCommandModelとして定義します。

* **[QueryModel](./020400_QueryModel/index.md)**
  * **データの一覧検索処理** です。主にGUIの一覧検索画面のために使用します。
  * 一覧検索はどのアプリケーションでも頻出し、かつパターンが決まっている（フィルタ、ソート、ページング）ため、処理本体まで自動生成されます。**CommandModelの処理本体の大部分を自動生成したもの** がQueryModelである、と言うこともできます。

:::note
**CQRS (Command Query Responsibility Segregation)** と用語が似ていますが、Nijoにおける定義はそれと必ずしも一致しません。
更新系処理と参照系処理を分離する考え方には通じるところもありますが、Nijoのモデル設計を進める上では意識しなくて構いません。
:::

## 画面とモデルの対応例

典型的な例として、デモ101の「売上一覧画面」と「売上詳細画面（新規登録または更新を行う画面）」を挙げます。

一覧検索画面では1つのQueryModelを、詳細編集画面では初期表示・新規登録・更新の3つのCommandModelを利用しています。
それら4つのモデルの内部では、売上・商品・従業員といったDataModelのモジュールを利用して処理を行っています。

![デモ101の売上関連画面のモデル構成](/img/model-collaboration.drawio.svg)

なお、画面UI自体はNijoの生成範囲外です。開発者は独自のUIを実装し、生成されたAPIクライアントを呼び出して利用します。
各モデルがフロントエンド・バックエンド・データベースのどこに配置されるかは [アーキテクチャ概要](../03_architecture.md) を参照してください。

## モデル間の関係

各モデルは独立して存在するのではなく、互いに関連し合っています。
Nijoでは以下の2つの関係性を定義できます。

* **参照 (Ref)**: 他のデータを指し示す関係（RDBMSの外部キーに近い）。
* **親子関係 (Child/Children)**: データの所有関係。親が消えれば子も消える、強い結びつき（集約の一部）。

---

次のページからは、各モデルの詳細な定義方法と役割について解説します。
