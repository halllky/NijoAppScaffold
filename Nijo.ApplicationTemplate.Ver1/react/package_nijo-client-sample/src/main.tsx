import React from 'react'
import * as ReactRouter from 'react-router-dom'
import ReactDOM from 'react-dom/client'
import { ContextProviders, getRouterForAutoGeneratedApplication } from './pages-reflection/MainLayout.Sidebar'

import './main.css'
import 'allotment/dist/style.css'
import QueryCommandDebugger from './reflection-debugger'

function App() {
  return (
    <ContextProviders>
      <div className="h-full w-full p-4">
        <QueryCommandDebugger className="h-full w-full border-2 border-purple-500" />
      </div>
    </ContextProviders>
  )

  const router = React.useMemo(() => {
    return ReactRouter.createBrowserRouter([getRouterForAutoGeneratedApplication()])
  }, [])

  return (
    <ReactRouter.RouterProvider router={router} />
  )
}

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
