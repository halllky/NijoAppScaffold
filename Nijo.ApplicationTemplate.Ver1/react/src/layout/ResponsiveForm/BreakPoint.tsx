import React from "react"

/**
 * 2段組みレイアウトの折り返し位置を決めるためのコンポーネント。
 * 具体的なDOM要素はレンダリングされない。
 * 1つの塊の中で複数回指定した場合、2つ目以降は無視される。
 * 未指定の場合、その塊に含まれる要素の数を2で割った位置で折り返す。
 */
export const BreakPoint = (): React.ReactNode => {
  return null
}